CREATE TABLE authorizations (
    auth_key VARCHAR(255) NOT NULL,
    user_id VARCHAR(255) NOT NULL,
    client_id VARCHAR(255) NOT NULL,
    scope VARCHAR(255) NOT NULL,
    UNIQUE(auth_key)
);
CREATE TABLE auth_key_log (
    auth_key VARCHAR(255) NOT NULL,
    UNIQUE(auth_key)
);
