<!DOCTYPE html>

<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>VPN Portal</title>
  <link rel="stylesheet" type="text/css" href="../css/default.css">
</head>

<body>
    <div class="container">
        <h1>VPN Portal</h1>
        <p>Welcome to the VPN portal. Here you can manage your VPN configurations.</p>

        <h2>New Configuration</h2>
        <p>Enter a name, e.g. &quot;Phone&quot;, for the configuration in the box below and click
           &quot;<em>Create</em>&quot;. <small>Allowed characters are <tt>a-z</tt>, <tt>A-Z</tt>, <tt>0-9</tt>, <tt>-</tt>, <tt>_</tt>, <tt>.</tt>, <tt>@</tt>.</small>
        </p>

        <form method="post">
            <input type="text" name="name" placeholder="Name..." required pattern="[a-zA-Z0-9-_.@]+">
            <input type="submit" value="Create">
        </form>

        {% if vpnConfigurations %} <!-- existing configurations -->
        <h2>Existing Configurations</h2>
        <p>If you lost your device or no longer use the VPN you can click &quot;<em>Revoke</em>&quot; to revoke the configuration.</p>
        <table>
            <thead>
                <tr><th>Configuration</th><th>Actions</th></tr>
            </thead>
            <tbody>
            {% for config in vpnConfigurations %}
                <tr>
                    {% if config.status == 30 %} <!-- revoked -->
                        <td><span class="revoked">{{ config.name|e }}</span></td>
                        <td>
                            <button disabled="disabled">Download</button>
                            <button disabled="disabled">Revoke</button>
                        </td>
                    {% elseif config.status == 20 %} <!-- active -->
                        <td>{{ config.name|e }}</td>
                        <td>
                            <button disabled="disabled">Download</button>
                            <form method="post" action="{{ config.name|e }}">
                                <input type="hidden" name="_METHOD" value="DELETE">
                                <input type="submit" value="Revoke">
                            </form>
                        </td>

                    {% else %}
                        <td>{{ config.name|e }}</td> <!-- ready -->
                        <td>
                            <form method="get" action="{{ config.name|e }}">
                                <input type="submit" value="Download">
                            </form>
                            <button disabled="disabled">Revoke</button>
                        </td>
                    {% endif %}
                </tr>
            {% endfor %}
            </tbody>
        </table>
        {% endif %} <!-- /existing configurations -->

    </div> <!-- /container -->
</body>
</html>
