<!DOCTYPE html>

<html lang="en">
{% include 'head.twig' %}
<body>
    {% include 'header.twig' %}

    <ul class="menu">
        <li class="active"><a href="">{% trans "Approval" %}</a>
    </ul>

    <p>{% trans "An application wants to act on your behalf." %}</p>

    <dl>
        <dt>{% trans "Application" %}</dt><dd>{{ client_id }}</dd>
        <dt>{% trans "Permissions" %}</dt><dd>{{ scope }}</dd>
        <dt>{% trans "Location" %}</dt><dd>{{ redirect_uri }}</dd>
    </dl>

    <form method="post">
        <button class="danger" type="submit" name="approve" value="no">{% trans "Reject" %}</button>
        <button class="success" type="submit" name="approve" value="yes">{% trans "Approve" %}</button>
    </form>

    {% include 'footer.twig' %}
</body>
</html>
